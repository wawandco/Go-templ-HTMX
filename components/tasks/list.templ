package tasks

import (
    "templ/models"
    "templ/components"
    )


templ List(tasks models.Tasks) {
    @components.Application() {
        <h1>Tasks</h1>
        <ul>
            for _, task := range tasks {
                <li>{ task.Title }</li>
            }
        </ul>
    }
}